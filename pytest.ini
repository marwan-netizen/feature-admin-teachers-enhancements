[pytest]
DJANGO_SETTINGS_MODULE = lingopulse.settings
python_files = tests.py test_*.py
python_classes = Test* !Test
# Removed --reuse-db to avoid unrecognized argument error if pytest-django is not handling it correctly
# or if it's causing issues in this environment.
addopts = -v
